<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bit.hellopt.data.LiveMapper">
	<select id="countClasses" resultType="int">
		SELECT COUNT(*) FROM CLASS_TB
	</select>
	
	<select id="PagingClassList" parameterType="int" resultType="com.bit.hellopt.vo.live.LiveClass">
		SELECT *
		FROM (SELECT ROWNUM R, C.*
		        FROM (SELECT *
		              FROM CLASS_TB
		              ORDER BY CLASS_IDX DESC
		             ) C
			)
		WHERE ROWNUM BETWEEN 1 AND #{end}
	</select>
</mapper>